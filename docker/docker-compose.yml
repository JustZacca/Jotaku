version: '3.8'

services:
  jotaku-server:
    build:
      context: ..
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    volumes:
      - jotaku-data:/data
    environment:
      - DB_PATH=/data/jotaku.db
      - JWT_SECRET=${JWT_SECRET}
      - PORT=8080
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:8080/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s

volumes:
  jotaku-data:
